

//its so called higher order component that we wrapped in person.js and render the children inbtwen tags
//children opening and closing tag vitra ko 

const aux = props => props.children;

export default aux;